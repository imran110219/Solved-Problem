#include <iostream>
#include <cstdio>
#include <cstring>

using namespace std;

int main()
{
    freopen("in.txt", "r", stdin);
    char equation[100];
    while(gets(equation))
    {
        printf("%s\n",equation);
    }
    return 0;
}
